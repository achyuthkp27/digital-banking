<!-- BEGIN:git-push-rules -->
# Do not push code unless explicitly asked
Never push code to the repository automatically. Wait for the user to explicitly say "push code" or "git push" before attempting to push.
<!-- END:git-push-rules -->
